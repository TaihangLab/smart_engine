# Smart Engine 开发环境配置示例
# 复制此文件为项目根目录的 .env.dev 并修改相应的配置
#
# 配置加载优先级：
# 1. .env 文件（基础配置，最高优先级）
# 2. .env.dev 文件（当 ENV=dev 时加载并覆盖）
# 3. 代码中的默认值（最低优先级）
#
# 注意：生产环境建议使用 Nacos 配置中心统一管理配置

# ===========================================
# 环境配置
# ===========================================
ENV=dev

# ===========================================
# Nacos 配置中心 (必需)
# ===========================================
# Nacos服务器地址
NACOS_SERVER_ADDRESSES=127.0.0.1:8848

# Nacos命名空间
NACOS_NAMESPACE=dev

# Nacos分组
NACOS_GROUP_NAME=DEFAULT_GROUP

# ===========================================
# Nacos认证（如果启用了认证）
# ===========================================
# NACOS_USERNAME=nacos
# NACOS_PASSWORD=nacos

# ===========================================
# 本地开发备用配置 (当Nacos不可用时使用)
# ===========================================
# 注意：这些配置应该在 Nacos 中管理，这里仅作为备用

# 数据库配置（备用）
MYSQL_SERVER=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DB=smart_vision

# Redis配置（备用）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=""
REDIS_DB=0

# RabbitMQ配置（备用）
RABBITMQ_HOST=127.0.0.1
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin

# 系统配置（备用）
DEBUG=true
LOG_LEVEL=DEBUG
REST_PORT=8000
# ===========================================
# 🚀 系统模块启用配置（可选）
# ===========================================
# 取消注释下面的配置项可以选择性禁用某些模块

# 核心系统模块
# SYSTEM_CORE_ENABLED=true          # 是否启用系统核心模块
# DATABASE_INIT_ENABLED=true        # 是否启用数据库初始化
# TRITON_MODEL_SYNC_ENABLED=true    # 是否启用Triton模型同步
# SKILL_MANAGER_ENABLED=true        # 是否启用技能管理器
# AI_TASK_EXECUTOR_ENABLED=true     # 是否启用AI任务执行器
# SSE_MANAGER_ENABLED=true          # 是否启用SSE连接管理器
# REDIS_CLIENT_ENABLED=true         # 是否启用Redis客户端
# LLM_TASK_EXECUTOR_ENABLED=true    # 是否启用LLM任务执行器

# 外部服务模块
# NACOS_REGISTRATION_ENABLED=true   # 是否启用Nacos服务注册
# MINIO_SERVICES_ENABLED=true       # 是否启用MinIO服务集群
# COMPENSATION_SERVICE_ENABLED=true # 是否启用补偿服务

# ===========================================
# 📋 配置说明
# ===========================================
# - 设置为 false 可以跳过相应模块的启动
# - 适用于开发环境或特定部署场景
# - 可以根据需要灵活组合启用/禁用模块
# - 生产环境建议保持默认配置
