1. 执行 python 命令前，先激活虚拟环境，使用 `source .venv/bin/activate` 激活虚拟环境。
2. 操作数据时禁止使用数据库主键ID（如修改、删除、查询等），必须使用业务唯一标识（如tenant_id、user_id等）。原因：
   - 避免可遍历漏洞，防止攻击者通过遍历ID获取敏感数据
   - 提高API安全性，减少水平越权风险
   - 即使有程序端的权限拦截，也应从根源上避免此类安全问题
   - 符合RESTful API设计最佳实践，使用有意义的业务标识符